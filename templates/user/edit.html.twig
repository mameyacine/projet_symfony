<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Edit User{% endblock %}</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    {% if theme == 'light' %}
        <link rel="stylesheet" href="{{ asset('css/theme-light.css') }}">
    {% else %}
        <link rel="stylesheet" href="{{ asset('css/theme-dark.css') }}">
    {% endif %}
</head>


<body>


    <nav class=" flex justify-between items-center px-4">
        <h1 class="m-2">Modifier un utilisateur</h1>
        <div>
            <a href="{{ path('user_index', { 'idA': admin_id }) }}" class="button"><i class="fas fa-arrow-rotate-left"></i></a>
        </div>
    </nav>



    
    <div class="mx-auto mt-8 max-w-lg p-4 rounded-lg shadow-lg overflow-hidden">
        {{ include('user/_form.html.twig') }}
    </div>
</body>
</html>