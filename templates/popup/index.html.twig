<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des cours disponibles</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    {% if theme == 'light' %}
        <link rel="stylesheet" href="{{ asset('css/theme-light.css') }}">
    {% else %}
        <link rel="stylesheet" href="{{ asset('css/theme-dark.css') }}">
    {% endif %}
</head>

<body>
    <nav class="px-4">
        <h1 class="m-2 text-center">Choisir mes premiers cours </h1>
    </nav>  


    <div class="container mx-auto mt-4 ">

    
    <form action="{{ path('process_popup_submission', {'idS': student_id}) }}" method="post" class="m-2">


               <ul class="flex flex-wrap">
                    {% for theme in themes %}
                        <li class="w-1/2"> 
                            <label>
                                <input type="checkbox" name="themes[]" value="{{ theme.id }}">
                                {{ theme.name }}
                            </label>
                         </li>
                    {% endfor %}
                </ul>

        <button type="submit" class="m-2 button">Valider</button>
    </form>
</div>
